# Implementation Plan: block-morphing-slice-1

L2 Slice A — single-paragraph Instant Morphing MVP.

## Tasks

1. OpenSpec artifacts + validate
2. `AetherMorphingContent` + `renderParagraphInline`
3. happy-dom integration tests (A/B/remount)
4. `examples/block-morphing` scaffold
5. `pnpm check` + archive + docs sync

All tasks executed in single session on `feat/block-morphing-slice-1`.
