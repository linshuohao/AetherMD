# @aether-md/example-react

React Shell demo — `AetherShellShowcase` switches between `AetherEditorContent` (L1) and `AetherMorphingDocument` (L2).

```bash
pnpm install   # repo root
pnpm build
pnpm --filter @aether-md/example-react dev
```

`private: true` — not published to npm.
