interface:
  display_name: "Aether Execute Spec Change"
  short_description: "Execute a Spec Change through single-task implement, validate, review, and archive"
  default_prompt: "Use $aether-workflow-execute-spec-change to execute the single task for this Spec Change."

policy:
  allow_implicit_invocation: true
